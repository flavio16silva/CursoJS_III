const numeros = [1, 2, 3, 4, 5]
const retorno = numeros[0] + numeros[1] + numeros[2]
console.log(retorno)
console.log(numeros.length)


const num = [1, 2, 3]
//num.forEach()
num.forEach(() => {
  console.log('Oi') 
})
